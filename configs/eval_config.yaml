run_name: "Run1-Baseline-Val-Sweep"
data_version: "v1.0-baseline"
split: "val"
pairs_file: "outputs/pairs/val.csv"
is_sweep: true
threshold: 0.5   # used if is_sweep is false
score_is_distance: false
note: "Initial threshold sweep on validation split"
output_dir: "outputs/eval"
